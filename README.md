# KnotLab

Draw a knot or link in the browser and compute Jones, HOMFLY-PT, Conway, and Alexander polynomials.

The public site is deployed with GitHub Pages from the workflow in `.github/workflows/pages.yml`.
